<h1>All cats</h1>

<ul>
  <% @cats.each do |cat| %>
      <li><%= link_to cat.name, cat_url(cat) %></li>
  <% end %>
</ul>

<%= link_to "Request a cat", new_cat_rental_request_url %><br>
<%= link_to "Add a cat", new_cat_url %>